import type { StructuredToolInterface } from '@langchain/core/tools';

/** Reserved for future QA pipeline tools (e.g. bindTools). */
export function getQATools(): StructuredToolInterface[] {
  return [];
}
