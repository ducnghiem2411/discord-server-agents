import type { StructuredToolInterface } from '@langchain/core/tools';

/** Reserved for future Manager pipeline tools (e.g. bindTools). */
export function getManagerTools(): StructuredToolInterface[] {
  return [];
}
